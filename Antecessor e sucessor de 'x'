#include <stdio.h>
int main(){
    float x,y,z;
    printf("Insira aqui um numero:");
    scanf("%f", &x);
    y = x-1;
    z = x+1;
    printf("Esse e o antecessor do seu numero: %.2f\nEsse e o sucessor do seu numero: %.2f",y,z);
    return 0;
}
