#include <stdio.h>
    int main(){
        char nome[30];
        printf("Insira aqui seu nome:");
        scanf("%s", &nome);
        printf("Ola, %s, seja muito bem vindo, seu nome e lindo!", nome);
        return 0;
    }
